#include<stdio.h>
#include<stdlib.h>


struct node{
	int data;
	struct *left,*right;
}

int newNode(int value)
{
	struct *node= (struct node *) malloc(sizeof(struct *node));
	node->data=value;	
	node->left=NULL;
	node->right=NULL;
	
